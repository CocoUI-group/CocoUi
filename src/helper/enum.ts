export enum ThemeType {
  default = "default",
  primary = "primary",
  secondary = "secondary",
  success = "success",
  warning = "warning",
  danger = "danger",
}

export enum ThemeSize {
  mini = "mini",
  sm = "sm",
  md = "md",
  lg = "lg",
}

export enum ThemeRound {
  none = "none",
  bySize = "bySize",
  full = "full",
}

export enum ThemeDirection {
  x = "x",
  y = "y",
}
